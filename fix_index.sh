sed -i 's|<head>|<head>\n    <script>\n      try {\n        const desc = Object.getOwnPropertyDescriptor(window, "fetch");\n        if (!desc) {\n          let proto = window;\n          let pDesc = null;\n          while (proto && !pDesc) {\n            pDesc = Object.getOwnPropertyDescriptor(proto, "fetch");\n            proto = Object.getPrototypeOf(proto);\n          }\n          if (pDesc && pDesc.get && !pDesc.set) {\n            Object.defineProperty(window, "fetch", {\n              get: pDesc.get,\n              set: function(val) { console.warn("Prevented window.fetch override"); },\n              configurable: true,\n              enumerable: true\n            });\n          }\n        } else if (desc.get && !desc.set) {\n          Object.defineProperty(window, "fetch", {\n            get: desc.get,\n            set: function(val) { console.warn("Prevented window.fetch override"); },\n            configurable: true,\n            enumerable: true\n          });\n        }\n      } catch (e) { console.error("Error fixing fetch:", e); }\n    </script>|g' index.html
